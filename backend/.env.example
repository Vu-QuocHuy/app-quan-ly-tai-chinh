# Never commit the real key. Configure with Firebase Secret Manager:
# firebase functions:secrets:set GEMINI_API_KEY
GEMINI_MODEL=gemini-2.5-flash
GEMINI_CHAT_MODEL=gemini-2.5-flash
# Set false to disable live exchange-rate enrichment without a redeploy.
ENABLE_EXTERNAL_EXCHANGE_RATES=true
